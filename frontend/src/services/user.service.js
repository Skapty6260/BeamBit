/**
 * @param {string} email (may using login and phone number)
 * @param {string} password user password (encoded)
 */
export const UserService = {
  async login(email, password) {
    return email, password;
    // backend code
  },
};
